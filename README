To build project run following commands starting from source dir:

mkdir ../build && cd ../build

# use source dir path as parameter
cmake ../test2

cmake --build .

Now you can run app with command:

./bridge_utility
