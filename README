To build project run following commands from source dir:

mkdir ../build && cd ../build
cmake ../test2                                      # use source dir path
cmake --build .

Now you can run appwith command:

./bridge_utility
